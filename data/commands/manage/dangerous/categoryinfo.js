class Category {
    constructor() {
        this.name = "Dangerous"
        this.description = "Commands that if you don't use correctly, will break the bot."
    }
}

module.exports.Category = Category