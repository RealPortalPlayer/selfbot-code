const Settings = require("../settings/botsettings")

function log(bot, message) {
	if (message.trim() && typeof message === "string") {
	}
}

function bug(bot, message) {
	if (message.trim() && typeof message === "") {
	}
}